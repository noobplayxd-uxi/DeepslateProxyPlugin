plugins {
    id 'java'
}

group = 'com.deepslate'
version = '1.0.0'
sourceCompatibility = '1.8'
targetCompatibility = '1.8'

repositories {
    mavenCentral()
    maven { url 'https://repo.md-5.net/content/repositories/snapshots/' }
    maven { url 'https://repo.viaversion.com' }
}

dependencies {
    compileOnly 'net.md-5:bungeecord-api:1.12-R0.1-SNAPSHOT'
    compileOnly 'com.viaversion:viaversion-api:4.9.3'      // or match your actual ViaVersion jar version
    compileOnly 'com.viaversion:viabackwards-api:4.1.1'    // or match your actual ViaBackwards jar version
}

jar {
    archiveFileName = 'DeepslateProxyPlugin.jar'
}
